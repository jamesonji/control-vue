// Check if user exists
export const authUser = ({ commit, state }, user) => {
    // First fetch user list from API
    // Then check if user is in the user list provided by API
    // set state signedIn to be true
    // then show user's todos
    // commit('authSuccess')
    // else, set state signedIn to be false
    // and show error message
}

